{
  "_comment": "Auto-binding mode demo: spec lists no individual test cases. The runner iterates top-level keys of `data_sources.inputs` (the 7 case_* entries) and for each one auto-builds vars from `inputs.{case}.{varname}` and expected from `expected.{case}.{formula_name}`. Adding new cases is a JSON-file edit, never a spec edit.",

  "formula_config": {
    "path":         "example_config.json",
    "exprs_at":     "expr",
    "constants_at": ["physics_consts", "calibration_consts"],
    "subexprs_at":  "subexprs"
  },

  "data_sources": {
    "inputs":   "example_inputs.json",
    "expected": "example_expected.json"
  },

  "compile_options": { "jit": false },
  "tolerance": 1e-9,

  "auto_test_cases": {
    "iterate":       "inputs",
    "vars_from":     "inputs.{case}",
    "expected_from": "expected.{case}"
  }
}
