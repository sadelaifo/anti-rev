{
  "_comment": "Test fixture for tools/expr_compiler.py — constants are name:number pairs that get baked in as literals; variable names are NOT listed and are auto-detected from the formula's surviving free symbols. Includes a 12-digit constant (typical engineering value, well within IEEE-754) to verify no rounding happens through the sympy + pycode pipeline.",

  "constants": {
    "k1":    6.5e-5,
    "k2":    0.001,
    "bias":  12,
    "scale": 0.5,
    "third": 0.833333333333
  },

  "subexprs": {
    "power": "v * i",
    "ohm":   "k1 * v**2",
    "iron":  "k2 * i**2",
    "loss":  "ohm + iron"
  },

  "formula": "scale*power - loss + bias + third*v",

  "_other_field": "unrelated keys are not walked or compiled"
}
