{
  "_comment": "Test fixture for tools/expr_compiler.py — constants split across two keys (physics_consts + calibration_consts) get merged at compile time. Variable names are NOT listed and are auto-detected from surviving free symbols. Includes a 12-digit constant to verify no rounding through sympy + pycode.",

  "physics_consts": {
    "k1":    6.5e-5,
    "k2":    0.001
  },
  "calibration_consts": {
    "bias":  12,
    "scale": 0.5,
    "third": 0.833333333333
  },

  "subexprs": {
    "power": "v * i",
    "ohm":   "k1 * v**2",
    "iron":  "k2 * i**2",
    "loss":  "ohm + iron"
  },

  "formula": "scale*power - loss + bias + third*v",

  "_other_field": "unrelated keys are not walked or compiled"
}
