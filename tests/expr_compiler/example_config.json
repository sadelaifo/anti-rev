{
  "_comment": "Example multi-formula config — formulas stored as a LIST (a[0], a[1], ...) the way real-world configs often do, not as a named dict. run_from_spec.py auto-keys them by stringified index ('0', '1', ...).",

  "physics_consts": {
    "k1":    6.5e-5,
    "k2":    0.001
  },
  "calibration_consts": {
    "bias":  12,
    "scale": 0.5,
    "alpha": 0.004,
    "T_ref": 25.0
  },

  "subexprs": {
    "power":  "v * i",
    "iron":   "k1 * v**2",
    "copper": "k2 * i**2",
    "loss":   "iron + copper"
  },

  "expr": [
    "scale * power",
    "loss",
    "scale*power - loss + bias",
    "1 + alpha * (t - T_ref)"
  ]
}
