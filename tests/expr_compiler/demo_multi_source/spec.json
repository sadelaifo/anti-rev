{
  "_comment": "Demo: vars_from is a LIST of container paths. For each case, the runner resolves every entry — each one must point to a dict — and merges all those dicts together. Net effect: case 'err' gets vars {v, t} from file1.err.a AND {i, p} from file2.err.b in a single combined vars dict.",

  "formula_config": {
    "path":         "formulas.json",
    "exprs_at":     "expr",
    "constants_at": "constants",
    "subexprs_at":  "subexprs"
  },

  "data_sources": {
    "file1": "file1.json",
    "file2": "file2.json",
    "ref":   "expected.json"
  },

  "compile_options": { "jit": false },
  "tolerance": 1e-9,

  "binding": {
    "vars_from": [
      "file1.{case}.a",
      "file2.{case}.b"
    ],
    "expected_from": "ref.{case}"
  },

  "test_cases": [
    "err",
    "warn",
    "info"
  ]
}
