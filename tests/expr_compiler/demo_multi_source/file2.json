{
  "_comment": "Source #2 — its containers are at different path names than file1 (vars2 / vars4 / vars6 instead of vars1 / vars3 / vars5). There's no global pattern that names both containers for a given case, so each test_case entry has to spell out which two containers belong together.",

  "vars2": { "b": { "i": 1.5, "p": 0.1 } },
  "vars4": { "b": { "i": 0.5, "p": 0.05 } },
  "vars6": { "b": { "i": 0.1, "p": 0.02 } }
}
