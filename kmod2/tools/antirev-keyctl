#!/bin/bash
# antirev-keyctl — load the antirevfs AES-256 key into the kernel keyring.
#
# The antirevfs module looks up a "user"-type key named "antirevfs:default" in
# the mounting process's keyrings.  This tool installs that key so `mount -t
# antirevfs` (run by the same user, normally root) finds it — keeping the key
# out of the mount command line and out of any process's heap after load.
#
# Usage:
#   antirev-keyctl unlock --keyfile <hexkeyfile>   # 64 hex chars (protect.py keyfile)
#   antirev-keyctl unlock-from-tpm                 # placeholder for TPM/dongle unseal
#   antirev-keyctl status                          # show whether the key is present
#   antirev-keyctl clear                           # revoke the key from the keyring
#
# Keyring: the session keyring (@s).  The kernel's request_key() from the mount
# path always searches the mounting task's session keyring, so the unlock and
# the mount must run in the same session (e.g. the same systemd unit / shell).
# The per-UID @u keyring is NOT reliable here: sudo/login (pam_keyinit) installs
# a fresh session keyring that does not link @u, so a key added to @u is invisible
# to request_key.  For an unlock-now / mount-later-in-another-session flow, prefer
# the key= mount option in fstab, or link @u into the unit's session keyring.
set -euo pipefail

DESC="antirevfs:default"
KEYRING="@s"

die() { echo "antirev-keyctl: $*" >&2; exit 1; }

find_key() { keyctl search "$KEYRING" user "$DESC" 2>/dev/null || true; }

cmd_unlock() {
	local keyfile=""
	while [[ $# -gt 0 ]]; do
		case "$1" in
			--keyfile) keyfile="${2:-}"; shift 2;;
			*) die "unknown arg: $1";;
		esac
	done
	[[ -n "$keyfile" ]] || die "unlock requires --keyfile <hexkeyfile>"
	[[ -r "$keyfile" ]] || die "cannot read keyfile: $keyfile"

	local hex
	hex="$(tr -d '[:space:]' < "$keyfile")"
	[[ "${#hex}" -eq 64 ]] || die "keyfile must contain 64 hex chars (32-byte AES-256 key), got ${#hex}"
	[[ "$hex" =~ ^[0-9a-fA-F]+$ ]] || die "keyfile is not hex"

	# padd reads the raw payload from stdin (binary-safe, unlike `add`).
	echo -n "$hex" | xxd -r -p | keyctl padd user "$DESC" "$KEYRING" >/dev/null
	echo "antirev-keyctl: key '$DESC' installed in $KEYRING"
}

cmd_unlock_from_tpm() {
	die "unlock-from-tpm not implemented — wire up your TPM/PKCS#11/dongle unseal here, then 'keyctl padd user $DESC $KEYRING'"
}

cmd_status() {
	local id; id="$(find_key)"
	if [[ -n "$id" ]]; then
		echo "antirev-keyctl: key '$DESC' present (id $id)"
	else
		echo "antirev-keyctl: key '$DESC' NOT present"
		return 1
	fi
}

cmd_clear() {
	local id; id="$(find_key)"
	[[ -n "$id" ]] || { echo "antirev-keyctl: no key to clear"; return 0; }
	keyctl revoke "$id" >/dev/null 2>&1 || true
	keyctl unlink "$id" "$KEYRING" >/dev/null 2>&1 || true
	echo "antirev-keyctl: key '$DESC' cleared"
}

[[ $# -ge 1 ]] || die "usage: antirev-keyctl {unlock --keyfile F|unlock-from-tpm|status|clear}"
sub="$1"; shift
case "$sub" in
	unlock)          cmd_unlock "$@";;
	unlock-from-tpm) cmd_unlock_from_tpm "$@";;
	status)          cmd_status "$@";;
	clear)           cmd_clear "$@";;
	*) die "unknown subcommand: $sub";;
esac
